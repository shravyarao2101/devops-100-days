# Day 01 — Commands Used

## Linux
pwd
ls -la
cd ~
mkdir practice
cd practice
touch file1.txt
cat file1.txt
rm file1.txt

## Git
git clone <repo-url>
git status
git add .
git commit -m "Day 1: Linux + Git + AWS fundamentals"
git push

## SSH
chmod 400 <key>.pem
ssh -i <key>.pem ubuntu@<public-ip>
